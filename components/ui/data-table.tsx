import type { ReactNode } from "react";
export type Column<T> = { key: string; label: string; render: (row: T) => ReactNode };
export function DataTable<T extends { id?: string | number }>({ rows, columns }: { rows: T[]; columns: Column<T>[] }) { return <div className="overflow-x-auto rounded-2xl border border-white/10"><table className="w-full text-left text-sm"><thead className="bg-white/5 text-xs uppercase tracking-wider text-umbra-muted"><tr>{columns.map((column) => <th className="px-4 py-3" key={column.key}>{column.label}</th>)}</tr></thead><tbody className="divide-y divide-white/5">{rows.map((row, index) => <tr className="text-umbra-lilac" key={row.id ?? index}>{columns.map((column) => <td className="px-4 py-3" key={column.key}>{column.render(row)}</td>)}</tr>)}</tbody></table></div>; }
